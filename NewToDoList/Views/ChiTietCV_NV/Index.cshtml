@model IEnumerable<NewToDoList.Models.ChiTietCV_NV>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create",new { id = ViewBag.id})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CongViec.TieuDe)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NhanVien.HoTen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TrangThai1.TinhTrang)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CongViec.TieuDe)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NhanVien.HoTen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TrangThai1.TinhTrang)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaCV,nv=item.MaNV }) |
            @*@Html.ActionLink("Details", "Details", new { id = item.MaCV, nv = item.MaNV }) |*@
            @Html.ActionLink("Delete", "Delete", new { id = item.MaCV, nv = item.MaNV })
        </td>
    </tr>
}

</table>
